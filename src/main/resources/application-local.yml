spring:
  # DB 연결정보(url/username/password)는 명시하지 않는다.
  # spring-boot-docker-compose가 compose.yaml + .env를 읽어 자동 주입한다.
  jpa:
    hibernate:
      ddl-auto: validate   # 스키마는 Flyway가 관리, Hibernate는 매핑 검증만
    show-sql: true
