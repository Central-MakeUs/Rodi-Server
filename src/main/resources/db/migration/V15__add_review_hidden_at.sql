-- 신고 누적 자동 비공개(스펙 010): 서로 다른 5명에게 신고되면 후기를 비공개 처리한다.
-- 판정 시각을 남겨 근거로 쓰고, 오탐이면 이 값을 NULL로 되돌려 복구한다(운영자 화면은 아직 없음).
-- 비공개 후기는 목록·요약에서 빠지고 작성자 본인 목록에만 남는다.
ALTER TABLE review ADD COLUMN hidden_at TIMESTAMP;
