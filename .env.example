# 로컬 개발 DB 설정 — 이 파일을 .env 로 복사해서 사용하세요 (cp .env.example .env)
# docker compose 와 스프링 local 프로파일이 이 값을 읽습니다.

POSTGRES_DB=rodi
POSTGRES_USER=rodi
POSTGRES_PASSWORD=rodi        # 로컬 개발용. 공유·운영 환경에선 반드시 변경
POSTGRES_PORT=5432

# 서버 오류(5xx) Slack 알림 — Slack Incoming Webhook URL. 비우면 알림 미전송.
SLACK_WEBHOOK_URL=
